server:
  port: ${SERVER_PORT:8081}

spring:
  application:
    name: telegram-app

telegram:
  bot:
    token: ${TELEGRAM_BOT_TOKEN}

app:
  rest:
    base-url: ${REST_APP_BASE_URL:http://localhost:8080}
